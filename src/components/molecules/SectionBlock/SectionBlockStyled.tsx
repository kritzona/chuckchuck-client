import styled, { css, DefaultTheme } from 'styled-components'

interface IProps {
  theme: DefaultTheme
}

export const SectionBlockStyled = styled.section<IProps>`
  ${() => css``}
`
export const SectionBlockHeaderStyled = styled.div<IProps>`
  ${() => css``}
`
export const SectionBlockContentStyled = styled.div<IProps>`
  ${() => css``}
`
export const SectionBlockFooterStyled = styled.div<IProps>`
  ${() => css``}
`
